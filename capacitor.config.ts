import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-mock-porto1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
